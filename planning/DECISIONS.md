# Decisions

Record durable decisions future sprints must respect.

---

## Decision Log

| Date | Decision | Reason | Impact |
|---|---|---|---|
| 2026-06-19 | Sprint 052 creates Outlook drafts only, never sends email. | The product remains human-in-the-loop and test-first. | No `Mail.Send` scope, no Microsoft Graph send endpoint, and no automatic sending behavior may be added. |
| 2026-06-19 | Sprint 052 uses delegated per-user Microsoft Graph access. | The initial use case is a user creating drafts in their own connected mailbox. | Do not use application permissions, shared mailbox access, or tenant-wide mailbox access in this sprint. |
| 2026-06-19 | Sprint 052 starts with one dedicated test Outlook mailbox. | Limits risk and simplifies validation. | Production or multi-user mailbox rollout needs a later approved sprint. |
| 2026-06-19 | Microsoft Copilot is excluded from Sprint 052. | Outlook draft creation must work and be validated before adding a second Microsoft integration. | Copilot requires a separate Architect Pack after Outlook acceptance. |
| 2026-06-19 | OAuth and Microsoft connection material must remain server-side and encrypted at rest. | Refresh tokens and client secrets are sensitive credentials. | Tokens must never be returned to the browser, committed, logged, or placed in audit metadata. |
| 2026-06-19 | Migration numbering must be derived from the actual repository. | The earlier Codex proposal incorrectly suggested `0010_microsoft_outlook_drafts.sql`, while prior work already used `0010_app_sessions.sql`. | Inspect migrations before creating any new migration file. |
| 2026-06-20 | Sprint 052 persists Microsoft OAuth state, encrypted connection material, and Outlook draft delivery tracking in a dedicated next-numbered D1 migration text file. | Outlook OAuth needs user-bound state, token refresh metadata, and idempotent draft delivery records without reusing provider-key storage. | `0011_microsoft_outlook_drafts.sql` must not be treated as applied until a separate approved D1 step runs it. |
| 2026-06-20 | Outlook testing pauses until local EmailORC authentication is proven. | The Microsoft connect route returned `Authentication required.` before OAuth could begin. | Sprint 053 must resolve local login/session/current-user behavior before resuming mailbox UAT. |
| 2026-06-20 | Sprint 053 is a corrective local auth/role sprint and does not expand Outlook scope. | The blocker is EmailORC local auth/admin behavior, not Microsoft Graph functionality. | Do not change Outlook scopes, send boundaries, Graph endpoint, or Microsoft configuration unless a direct compatibility defect is proven. |
| 2026-06-20 | Client-facing account-owner role remains `client_admin` unless repository evidence proves another canonical model. | The pack explicitly rejects creating a new `client` role without repo evidence. | Role UI and save behavior should clarify `client_admin`, `editor`, `viewer`, and `super_admin`. |
| 2026-06-20 | Session cookie `Secure` behavior must follow the actual request protocol, not only `NODE_ENV`. | Wrangler preview runs a production build over local HTTP; Secure cookies are not sent back on HTTP. | Local preview sessions can work while HTTPS deployments still receive Secure cookies. |
| 2026-06-20 | Demo-mode Super Admin bootstrap is allowed only for the documented demo Super Admin when local D1 lacks that row. | Local D1 had `app_sessions` but lacked `admin@demo.com`, blocking known local Super Admin login. | Do not bootstrap `client_admin` users automatically; keep bootstrap constrained to demo Super Admin troubleshooting. |
| 2026-06-20 | Sprint 053-A requires demo Super Admin bootstrap to be local-only, not merely `APP_ENV=demo`. | Remote/deployed demo can also have `APP_ENV=demo`, so environment intent alone is not a safe bootstrap boundary. | Add an explicit local-runtime guard that defaults false when uncertain. |
| 2026-06-20 | Sprint 053-A must prevent final Super Admin self-demotion, self-deactivation, and self-archive. | Admin updates must not accidentally remove all platform-owner access. | User update/delete routes need final-Super-Admin guard tests before commit. |
| 2026-06-20 | Sprint 053-A must make organization-boundary behavior explicit for user updates. | Read-only review found admin PATCH can update the first membership for any user without explicit same-org/cross-org policy. | Normal org-bound management must reject cross-org targets; global Super Admin policy must be explicit and tested. |
| 2026-06-20 | Demo Super Admin bootstrap now requires `APP_ENV=demo` plus a local request host. | `APP_ENV=demo` alone is insufficient because deployed demo runtimes can share that value. | Bootstrap is blocked for unknown/non-local hosts and can create only `admin@demo.com`. |
| 2026-06-20 | Admin user updates use the authenticated Super Admin's current organization membership. | Updating the first membership blindly can mutate the wrong organization. | Same-org role updates work; cross-org updates are rejected in the current MVP admin path. |
| 2026-06-20 | Admin user routes must preserve at least one active `super_admin`. | Removing the final platform owner can lock out administration. | Final active Super Admin demotion, deactivation, and archive/delete are rejected. |
| 2026-06-20 | Sprint 054 must diagnose the local Outlook callback worker failure before implementation. | Microsoft OAuth appears to have redirected back locally, but the browser reported `ERR_CONNECTION_CLOSED`. | Do not change callback/runtime code until terminal evidence identifies crash, exit, restart, port loss, or route exception. |
| 2026-06-20 | Sprint 054 must not change Outlook scopes or send boundaries. | The observed issue is local callback/worker stability, not an approved change to Microsoft permissions or draft behavior. | Keep no `Mail.Send`, no `/send`, no `/sendMail`, and no Sprint 052 PASS claim. |
| 2026-06-20 | Sprint 055 must reconcile the displayed Outlook mailbox hint before Outlook Drafts UAT continues. | Microsoft account notification and EmailORC card currently disagree. | Do not treat the Integrations card as identity proof until its source is corrected. |
| 2026-06-20 | EmailORC must not use the local app user email as the connected Outlook mailbox identity. | The app user can differ from the delegated Microsoft mailbox. | Connected mailbox hint must come from Microsoft-authorized identity data or be shown as unknown. |
| 2026-06-20 | Sprint 055 must not add Microsoft permissions just to display a mailbox hint. | Current delegated OAuth already requests OIDC identity scopes. | Prefer safe ID token claims if available; do not add `Mail.Send` or broaden Graph access. |
| 2026-06-20 | Outlook connected mailbox hints are derived only from safe Microsoft `id_token` claims for new connections. | The callback previously stored the EmailORC app user email as the Microsoft mailbox hint. | Use `email`, then `preferred_username`, then `upn` when valid; store/display only the redacted hint; store no invented hint when claims are missing or invalid. |
| 2026-06-20 | Sprint 055 does not backfill existing Microsoft `account_hint` values. | The approved scope excluded cleanup, migration, and backfill work. | Previously stored incorrect hints can persist until a future successful reconnect or approved cleanup sprint replaces them. |
| 2026-06-20 | Draft approval UI must not trust raw `localStorage.userRole` casing as an authorization decision. | Live UAT showed canonical `super_admin` blocked by an uppercase-only client check. | UI affordances must use canonical role normalization and/or server current-user data. |
| 2026-06-20 | Server approval authorization remains authoritative. | Client checks are UX affordances only, not security boundaries. | Sprint 056 must not weaken server route guards or organization authorization. |
| 2026-06-20 | Outlook Draft UAT must use a real approved D1 draft row. | The Outlook draft API reads D1 drafts and cannot rely on demo/localStorage-only draft state. | Sprint 056 must make the UAT path explicit and testable before resuming draft-only Outlook UAT. |
| 2026-06-20 | Dashboard mock-data reset is deferred. | It is unrelated to the immediate approval UI and Outlook-draft blocker. | Plan reset/clear-before-live as a separate future Architect Pack. |
| 2026-06-20 | Sprint 058 is limited to the server-session bootstrap/auth-state mismatch. | The browser displays `Super Admin` while protected APIs return `401 Authentication required`. | Do not broaden into Outlook functionality, OAuth configuration, page guard overhaul, or general auth redesign. |
| 2026-06-20 | Server session remains authoritative. | Local browser identity/display state cannot authorize protected APIs. | Do not weaken server guards to make the UI appear to work. |
| 2026-06-20 | Microsoft Connect must not be treated as an available action without a valid server session. | The integration start route returned `401`. | The UI must establish or require legitimate authentication before starting protected integration actions. |
| 2026-06-20 | Sprint 058 validation must not create Outlook drafts or send email. | The task is session/auth repair, not Outlook UAT completion. | Microsoft OAuth start may be inspected only if safe and explicitly approved after session behavior is validated. |
| 2026-06-20 | Local Prisma-fallback sessions must use a durable server-resolvable local persistence path, not module-level memory alone. | Next dev/App Router module behavior makes in-memory-only lookup unreliable across routes/reloads. | Login, `/api/auth/me`, logout, and current-user lookup must share the same local session store. |
| 2026-06-20 | Browser localStorage remains display/compatibility state only and must not control `/login` authenticated redirect decisions. | Sprint 058 and local UAT showed stale localStorage can create false login success/redirect loops. | Login redirect must require server-authenticated `/api/auth/me` confirmation. |
| 2026-06-20 | D1 remains the deployed/test-live session direction; Prisma fallback is local-development support only. | Sprint 008-012 direction remains unchanged. | Sprint 059 must not redefine deployed production session architecture. |
| 2026-06-21 | Sprint 059 selected `.next/cache/emailorc-local-sessions.json` as the local-development-only fallback session store. | The existing Prisma schema has no compatible session model and Sprint 059 prohibited Prisma/D1 schema changes or migration execution. | Local fallback sessions store token hashes and session metadata only, never plaintext session tokens; the store is non-production, ignored by Git through `.next`, and does not change Prisma or D1 schemas. |
| 2026-06-21 | Sprint 060 validated Microsoft integration storage readiness only through local Cloudflare/OpenNext with local Wrangler D1. | Plain local Next dev lacks the D1 binding and reports `storageAvailable:false`, while local Wrangler preview exposes `env.DB` for `emailorc-demo-db` and returned `storageAvailable:true`. | Future Outlook readiness validation should use Cloudflare/OpenNext with local or explicitly approved non-production D1; do not add Prisma, SQLite, JSON-file, or in-memory Microsoft connection storage fallbacks. |
| 2026-06-21 | Sprint 062 local Microsoft OAuth UAT requires exact HTTP localhost redirect alignment. | Wrangler preview runs at `http://localhost:8787`, and a prior HTTPS localhost callback caused `ERR_SSL_PROTOCOL_ERROR`. | Local Entra redirect URI and local-only `MICROSOFT_REDIRECT_URI` must both be `http://localhost:8787/api/integrations/microsoft/callback`; do not change production Entra/config for this sprint. |
| 2026-06-21 | Sprint 062 is connection-only preparation and does not authorize UAT execution yet. | The current phase is redirect/config alignment, not mailbox workflow validation. | Do not run preview, authenticate, click Connect, complete OAuth, create Outlook drafts, send email, deploy, commit, or push until separately approved. |
| 2026-06-21 | Sprint 062 callback return URLs must use the configured local redirect origin when present. | The callback handler previously derived `/mvp/integrations` from `request.url`, which could inherit an HTTPS-normalized localhost request and trigger `ERR_SSL_PROTOCOL_ERROR` under local Wrangler preview. | `MICROSOFT_REDIRECT_URI` is canonical for local OAuth callback origin; preserve request-derived behavior only when no configured redirect URI exists. |
| 2026-06-21 | Outlook draft UI availability must include recipient readiness. | Sprint 062 reached the draft action, but three attempts returned HTTP `400` because the selected approved D1-backed draft had a malformed recipient value. | `Create Outlook Draft` must not be shown or enabled until recipient email is present and valid; server-side validation remains final enforcement. |
| 2026-06-21 | Sprint 063 must not auto-correct recipient data. | The right correction workflow is a product decision: edit in EmailORC, fix source/import data, or select another record. | Builder may add blocked/readiness states, but must not mutate recipient data automatically. |
| 2026-06-22 | Sprint 063 recipient readiness uses safe categories rather than exposing recipient values. | Missing or malformed recipient state should explain why Outlook draft creation is blocked without displaying the full address. | UI and tests use `ready`, `missing_recipient`, and `invalid_recipient`; server-side validation still blocks bad input before Microsoft Graph. |
| 2026-06-22 | Human-approved correction record is authoritative for corrected recipients. Preserve the original approved import/source value in audit history. | Sprint 065 approved the recipient source-of-truth policy before any correction workflow or UAT fixture implementation. | Future recipient correction work must treat the approved correction record as authoritative while retaining the original import/source value for audit. |
| 2026-06-22 | A sales user may propose a correction. An admin must approve it before it becomes authoritative. | Sprint 065 approved a two-step correction authority model. | Future implementation must separate proposal from approval and must not let sales-user proposals directly change the authoritative recipient. |
| 2026-06-22 | Correct the EmailORC client record. Do not use draft-only replacement as the permanent correction location. | Sprint 065 selected the EmailORC client record as the correction location. | Future implementation should update the EmailORC client record through an approved/audited correction workflow rather than permanently patching only draft content. |
| 2026-06-22 | Recipient corrections must record old recipient value, new recipient value, actor, timestamp, correction reason, approval actor, approval timestamp, and related client/draft identifiers where applicable. | Sprint 065 defined the required audit trail for recipient correction. | Future implementation must provide this audit record while continuing to avoid exposing full recipient values in logs/docs/chat outside approved secure storage. |
| 2026-06-22 | Already-approved drafts must never be edited. After an approved recipient correction, create a replacement draft if needed and preserve the original approved draft unchanged. | Sprint 065 approved immutable approved drafts as the safe audit posture. | Future correction work must create replacement drafts when needed and keep existing approved drafts unchanged. |
| 2026-06-22 | Future Outlook UAT fixtures must use only a dedicated internal Microsoft 365 test mailbox. | Sprint 065 approved the safe mailbox class for later non-client UAT fixture work. | Future UAT fixture creation must not use real client recipients or personal/non-dedicated mailboxes. |
| 2026-06-22 | Allow only one Outlook draft per approved test case, dedicated test mailbox, and test run. Duplicate attempts must be blocked or clearly flagged before creation. | Sprint 065 approved the duplicate-prevention policy for later Outlook draft UAT. | Future implementation must include idempotency or a pre-creation duplicate warning/block for the approved test case, mailbox, and run. |
| 2026-06-22 | Future safe Outlook Draft UAT fixture creation must default to the normal D1-backed import and approval workflow. Direct D1 edits are not approved as the default fixture path. | Sprint 066 read-only audit found the normal workflow can create D1-backed leads/drafts and approvals, but fixture creation remains future-gated. | A later sprint must get explicit approval before any D1 import, approval, Outlook draft creation, or exceptional direct D1 edit path. |
| 2026-06-22 | Sprint 066 does not prove duplicate prevention sufficient for Outlook Draft UAT. | The current delivery persistence can upsert an `outlook_draft_deliveries` record after Graph creation rather than proving a pre-creation duplicate block. | Sprint 067 must include duplicate-prevention design and proof before any Outlook draft UAT is approved. |
| 2026-06-28 | Lead deletion in Sprint 069 is archive/hide behavior, not destructive delete. | Leads can be referenced by drafts and import history, and the current schema has no explicit deleted table or approved hard-delete workflow. | Normal lead lists exclude `validation_status = 'ARCHIVED'`; import/source relationships remain preserved. |
| 2026-06-28 | Completed imports are not cancellable in the current workflow lifecycle. | `/api/workflow/import` creates the batch, leads, drafts, and analytics in one immediate operation and `import_batches` has no cancellable status. | Sprint 069 may cancel only staged uploads before persistence. A future sprint must design a real batch lifecycle before post-save cancellation. |
| 2026-06-28 | Manual leads created from the Records UI must use a manual-entry source label and must not create drafts automatically. | Recipient readiness needs lead data correction without expanding into AI generation or Outlook Draft behavior. | Manual creation writes a D1 lead only, requires a valid email, and leaves draft creation/approval unchanged. |
| 2026-06-28 | Sprint 070 is design-only and does not approve import lifecycle implementation. | Completed-import cancellation, archival, rollback, and restore behavior require explicit business approval before code or schema changes. | Do not add migration, route, UI, or data behavior for import lifecycle until a later approved implementation sprint. |
| 2026-06-28 | Staged upload cancellation is distinct from completed-import cancellation, archival, rollback, and deletion. | Sprint 069 cancellation is pre-persistence only; completed imports already have persisted leads/drafts/import/audit relationships. | Future UI and APIs must not present completed imports as cancelable without a real lifecycle/status model and audit behavior. |
| 2026-06-28 | Demo fallback records must remain visibly distinct from persisted/imported leads. | Medical-related demo records such as BluePath Health can appear without proving D1 import persistence. | UI/source metadata must label demo fallback records as non-persisted fallback data and must not use them as Outlook Draft UAT fixtures. |
| 2026-06-28 | Sprint 071 is policy decision capture only and does not approve import lifecycle implementation. | Completed-import behavior, lead archive behavior, lifecycle permissions, required reasons, audit storage, lead archive data model, and rollback timing remain founder approval decisions. | Sprint 072 must not begin until the Sprint 071 founder approval section is answered; all recommendations remain Pending Founder Approval. |
| 2026-06-28 | The recommended next implementation sprint is `072-import-lifecycle-status-archive-audit-implementation`. | Sprint 071 is decision capture, so the implementation sequence must move to the next sprint number. | Sprint 072 should implement only founder-approved lifecycle model, permissions, audit events, and validation coverage. |
| 2026-06-28 | Sprint 072 approved archive-only completed imports and deferred rollback. | The safe defaults explicitly prohibit destructive rollback and direct D1/manual cleanup. | Completed imports may be archived/restored with audit, but completed-import cancel and rollback remain out of scope. |
| 2026-06-28 | Sprint 072 lead archive is reversible and uses dedicated archive fields when the D1 migration is applied. | Validation status and archive lifecycle are separate concepts, and restore requires actor/reason metadata. | `0012_import_lead_lifecycle.sql` adds lead archive/restore fields; compatibility code still handles legacy `validation_status = 'ARCHIVED'` rows. |
| 2026-06-28 | Sprint 072 lifecycle actions use existing `audit_log`. | Existing audit storage can represent actor, organization, target, action, timestamp, and safe JSON metadata. | Staged cancel, import archive/restore, and lead archive/restore write lifecycle audit events without creating a second audit system. |
| 2026-06-28 | Sprint 072 requires lifecycle reasons for staged cancel, import archive/restore, and lead archive/restore. | Mandatory safe reason categories make cleanup and restore actions reviewable without exposing raw source data. | API routes reject missing or unknown lifecycle reasons. |
| 2026-06-28 | Medical/demo leads such as BluePath Health are demo fallback data unless app-visible source/import metadata proves persisted D1 origin. | Repo evidence shows BluePath Health in hardcoded MVP records fallback and campaign-board seed data. | Demo fallback records are labeled `Demo fallback` and must not be used as persisted-import proof or Outlook Draft UAT fixtures. |
| 2026-05-21 | Sprint 015 will harden only Brain / provider API organization permission guards. | Sprint 014 completed workflow/draft guard hardening; Brain/provider routes are the next focused organization-scoped route group. | Codex must not expand into billing, usage, account, middleware, page guards, localStorage cleanup, or production-readiness work. |
| 2026-05-21 | Brain / provider API authorization must derive organization and user context from the server-authenticated current-user helper. | Sprint 010 found request-controlled identity values were production blockers. | Brain/provider routes must not trust client-provided organization/user/role/provider/actor values for authorization. |
| 2026-05-21 | Brain / provider guards should return `401` for unauthenticated requests and `403` for authenticated users who are not authorized for the requested organization-scoped resource. | Clear auth failure semantics make future route hardening and tests consistent. | Tests should assert missing session, wrong-organization/forbidden, authorized behavior, and fail-closed behavior. |
| 2026-05-21 | Sprint 015 must not apply or modify the Sprint 012 D1 session migration. | Migration execution is a separate database operation and was intentionally deferred. | Codex must not run migrations, seed commands, D1 writes, Prisma commands, or deployment commands. |
