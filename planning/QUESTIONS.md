# Open Questions

| Question | Owner | Needed By | Status | Answer / Notes |
|---|---|---|---|---|
| Which Microsoft account will serve as the dedicated test mailbox? | Owner | Before OAuth testing | Open | Use one test mailbox only for Sprint 052. |
| Is the Entra app allowed to support personal Microsoft accounts, work/school accounts, or both? | Owner / Microsoft admin | Before implementation | Open | Default recommendation: support accounts in any organizational directory and personal Microsoft accounts, subject to the actual tenant/app-registration options. |
| Is user consent allowed for the chosen Microsoft tenant? | Owner / Microsoft admin | Before OAuth testing | Open | If tenant policy blocks user consent, an admin must grant consent for the delegated permission. |
| Where should encrypted connection material live in the deployed D1 design? | Architect / Builder | Before implementation | Open | Prefer a minimal D1-backed integration-connection table with encrypted payloads and rotation-ready key handling, if no existing secure store is already present. |
| Does the current runtime have a secure encryption-key binding pattern? | Builder | Preflight | Open | Inspect binding names/contracts only. Do not reveal secret values. |
| Is local validation using Next dev server, Wrangler local runtime, or a deployed test-live URL? | Owner / Builder | Before manual QA | Answered | Sprint 060 validated the local Cloudflare/OpenNext Wrangler runtime at `http://localhost:8787` with local D1 binding; plain Next dev remains unsuitable for Microsoft storage readiness because it lacks D1. |
| When should `0011_microsoft_outlook_drafts.sql` be applied to local/test D1? | Owner / Builder | Before manual OAuth QA | Answered for local | Sprint 060 approved local D1 migration/seed only. `npm run db:migrate` targeted `emailorc-demo-db --local --env demo` and found no pending migrations, meaning local migration state already included the required Microsoft tables. Remote/test-live application still requires separate approval. |
| Did Wrangler crash, exit, restart, lose port `8787`, or throw a callback/runtime exception after Microsoft OAuth callback? | Builder | Sprint 054 diagnosis | Open | Requires active `npm run preview` terminal output around the failure. |
| Did Microsoft OAuth complete before the local failure? | Builder | Sprint 054 diagnosis | Open | Browser reportedly reached `http://localhost:8787/mvp/integrations?microsoft=connected`, suggesting callback success, but terminal evidence must confirm. |
| Did the callback/token-storage path print any token, authorization code, client secret, raw Microsoft response, or email body? | Builder | Sprint 054 diagnosis | Open | Must be checked from terminal/browser output without inspecting `.dev.vars`. |
| Is the callback failure caused by callback route code, Microsoft helper code, D1 schema/binding state, encryption config, session/cookie behavior, redirect URI/port mismatch, or returned page load behavior? | Builder | Sprint 054 diagnosis | Open | Classify before implementation. |
| Why does Microsoft show the intended Outlook account while EmailORC displays `ad***@demo.com`? | Builder | Sprint 055 diagnosis | Answered | The Microsoft callback stored `currentUser.email` as `account_hint`, and connection storage also fell back to the EmailORC user email. |
| Which source currently supplies the EmailORC connected mailbox hint? | Builder | Sprint 055 diagnosis | Answered | `integration_connections.account_hint`, previously populated from the EmailORC current user email during callback/storage. |
| Which Microsoft-authorized identity source can be used without adding permissions? | Builder | Sprint 055 diagnosis | Answered | Safe `id_token` claims from the delegated OAuth token response, using `email`, then `preferred_username`, then `upn`. |
| Does Sprint 055 require a migration? | Builder | Sprint 055 diagnosis | Answered | No. Existing `account_hint` stores the safe redacted hint, and missing/invalid Microsoft identity stores an empty hint. No cleanup/backfill was added. |
| Which current Drafts-list records are real D1 rows versus demo/localStorage-only rows? | Builder | Sprint 056 | Active | Builder must identify the safe real-D1 UAT path without mutating data. |
| Does Drafts page already have a current-user fetch pattern that can be reused? | Builder | Sprint 056 | Active | Prefer existing session/current-user contract over new auth plumbing. |
| Does approval UI need a new explicit state for unavailable server session? | Builder / Architect | Sprint 056 | Active | Only add if required for clear user feedback; avoid redesign. |
| What exact normal-browser code path displays `Super Admin` without a session cookie? | Builder | Sprint 058 | Answered | `src/components/layout/Shell.tsx` accepted localStorage `userId`, `userEmail`, and `userRole` as enough to render protected MVP pages. |
| Does an existing explicit login screen correctly create the `emailorc_session` cookie locally? | Builder | Sprint 058 | Answered | Existing focused tests prove a real `emailorc_session` resolves `/api/auth/me`; Sprint 058 tests prove a valid server session reaches Microsoft Connect's existing storage/config behavior. |
| Should unauthenticated local users be redirected to login or shown a clear reconnect/sign-in message before protected actions? | Architect/Builder | Sprint 058 | Answered | The shell redirects stale localStorage-only users to login, while Integrations/Drafts disabled states explain sign-in is required if those pages render during session checks. |
| After session repair, is an approved D1-backed draft available for Outlook draft-only UAT? | Owner/Builder | Post-Sprint 058 | Open | Separate from session repair. |
| Is the local Prisma fallback session path durable enough for UAT? | Builder | Sprint 059 | Answered | Yes for local dev session continuity. Sprint 059 stores local fallback sessions in `.next/cache/emailorc-local-sessions.json` with token hashes and server session metadata. |
| Does local `npm run dev` now produce a valid server-authenticated session after demo login? | Builder | Sprint 059 validation | Answered | Yes. Local browser validation showed `admin@demo.com` login reaches `/mvp` and `/api/auth/me` returns `200` for `admin@demo.com` immediately after login. |
| Can Outlook UAT proceed after valid session and approved D1-backed draft conditions are met? | Owner/Builder | Post-Sprint 059 | Open | Still requires approved manual Microsoft path, a qualifying approved D1-backed draft, draft-only proof, and no-send mailbox evidence. |
| What storage/configuration is required for the Microsoft integration readiness gate to report `storageAvailable:true`? | Builder/Architect | Sprint 060 | Answered | Use Cloudflare/OpenNext through local Wrangler with local D1 binding `env.DB` for `emailorc-demo-db`, plus applied existing D1 migrations and approved demo seed. Sprint 060 status returned `200` with `storageAvailable:true`, `connected:false`, and `reconnectRequired:false`. |
| What exact local Microsoft OAuth callback must be registered for Wrangler preview? | Owner / Microsoft admin | Sprint 062 | Answered | `http://localhost:8787/api/integrations/microsoft/callback` must be registered in the local Microsoft Entra app configuration and matched by local-only `MICROSOFT_REDIRECT_URI`. |
| Has the Microsoft Entra admin/operator confirmed the local redirect URI was added? | Owner / Microsoft admin | Before Sprint 062 connection-only UAT | Open | Required before running preview, authenticating, clicking Connect, or completing OAuth. |
| Has the owner approved connection-only UAT execution after redirect alignment? | Owner | Before Sprint 062 UAT execution | Open | Separate approval is required before `npm run preview`, local authentication, Outlook / M365 Connect, or Microsoft OAuth completion. |
| How should users correct a missing or malformed recipient for Outlook draft readiness? | Owner / Architect | Before adding correction workflow | Answered | Human-approved correction record is authoritative for corrected recipients; preserve the original approved import/source value in audit history. A sales user may propose a correction, and an admin must approve it before it becomes authoritative. Correct the EmailORC client record; do not use draft-only replacement as the permanent correction location. |
| How should EmailORC create or identify a safe valid-recipient D1-backed draft for browser UAT? | Owner / Architect | Before Sprint 064 UAT retry | Open | Sprint 065 approved the policy path, but no fixture was created. A future explicitly approved non-code UAT-fixture preparation or implementation pack must identify or create a D1-backed safe test draft using only a dedicated internal Microsoft 365 test mailbox. |
| Who may correct a missing or malformed recipient and under what approval authority? | Owner / Architect | Sprint 065 | Answered | A sales user may propose a correction. An admin must approve it before it becomes authoritative. |
| Can an already-approved draft ever be edited after recipient correction? | Owner / Architect | Sprint 065 | Answered | Already-approved drafts must never be edited. After an approved recipient correction, create a replacement draft if needed and preserve the original approved draft unchanged. |
| What audit trail is required for recipient correction? | Owner / Architect | Sprint 065 | Answered | Record old recipient value, new recipient value, actor, timestamp, correction reason, approval actor, approval timestamp, and related client/draft identifiers where applicable. Preserve the original approved import/source value in audit history. |
| What internal mailbox or mailbox class is approved for safe Outlook Draft UAT fixtures? | Owner / Architect | Sprint 065 | Answered | Future Outlook UAT fixtures may use only a dedicated internal Microsoft 365 test mailbox. |
| How should the one-time valid-recipient Outlook Draft UAT avoid duplicate drafts? | Owner / Architect | Sprint 065 | Answered | Allow only one Outlook draft per approved test case, dedicated test mailbox, and test run. Duplicate attempts must be blocked or clearly flagged before creation. |
| What exact D1-backed draft schema and lifecycle requirements define a valid Outlook Draft UAT fixture candidate? | Builder | Sprint 066 | Answered | Sprint 066 verified the candidate requires D1-backed `leads.contact_email`, a paired non-archived approved `drafts` row with valid subject/body, approval evidence in `approvals`/`audit_log`, active Microsoft connection readiness, and delivery-tracking awareness. |
| Can one qualifying fixture be created or identified through the existing normal app workflow without direct database manipulation? | Builder | Sprint 066 | Answered | Feasible only through the normal D1-backed import and approval workflow with explicit future approval. Direct D1 edits are not approved as the default path. |
| What redacted dedicated internal Microsoft 365 test mailbox details are enough to approve a later fixture sprint? | Owner / Microsoft admin / Builder | Sprint 067 | Open | Need approved internal test mailbox label/class, test ownership, no-external-recipient confirmation, and draft-only/no-send authorization without documenting full address, credentials, tenant identifiers, tokens, or secrets. |
| What remaining approval gates must be satisfied before fixture creation can be proposed? | Owner / Builder | Sprint 067 | Answered | Sprint 067 defines the required written approval packet: approved internal mailbox label/class, internal-only draft-only confirmation, exact test case and test-run identity, source lead/import identity rules, recipient-correction approach if needed, normal D1-backed flow proof, pre-Graph duplicate block, no-send proof, audit evidence, and explicit approval for later D1 import, approval, and Outlook draft creation. |
| What exact evidence must a later fixture-creation sprint capture without exposing sensitive values? | Owner / Builder | Before fixture creation | Answered | Sprint 067 requires import evidence, approval evidence, Outlook draft-created/no-send evidence, and duplicate-prevention evidence proving a second attempt is blocked before Graph is called. Do not document full recipient values, full mailbox addresses, tokens, Graph responses, tenant identifiers, or message content. |
| Should malformed-recipient drafts show a disabled button, a replacement blocked badge, or an inline readiness notice? | Owner / Builder | Sprint 063 implementation | Answered | Sprint 063 uses a replacement blocked badge for approved D1-backed drafts whose recipient is missing or invalid, without exposing the full address or allowing the UI action to call `/api/drafts/[draftId]/outlook`. |
| Should recipient readiness be exposed as a reusable helper shared by Drafts UI and tests, or only as a Drafts-page helper aligned to server validation? | Builder | Sprint 063 implementation | Answered | Sprint 063 uses a small shared helper in `src/lib/draft-approval.ts`, backed by the Graph recipient email validator, so UI readiness and server validation stay aligned. |
| What is the safe pre-live process to clear demo dashboard counts and seeded data? | Architect / Owner | Future reset sprint | Open | Separate future Architect Pack. |
| Why did `/api/integrations/microsoft/connect` return `Authentication required.` under local Wrangler testing? | Builder | Sprint 053 implementation approval | Answered | Production-built preview set a Secure cookie on local HTTP, so the cookie was not sent back; local D1 also lacked the documented demo Super Admin row. |
| Is the local Super Admin user attached to the intended organization and active membership? | Builder | Sprint 053 implementation | Answered | Sprint 053 bootstraps the documented demo Super Admin into `org_demo` with `super_admin` only when missing in demo mode. |
| Why did Super Admin role updates fail to save locally? | Builder | Sprint 053 implementation | Answered | Without a recognized session, admin PATCH was not authorized. After session fix, same-org role update saved and persisted canonical `client_admin`. |
| Should local user creation require explicit role selection or keep a documented default role? | Owner / Builder | Sprint 053 implementation | Answered | Admin UI keeps explicit role selection with default `EDITOR`; no automatic `client_admin` assignment was added. |
| What exact local runtime signal should gate demo Super Admin bootstrap? | Builder | Sprint 053-A implementation | Answered | Bootstrap requires `APP_ENV=demo` and a local request host of `localhost`, `127.0.0.1`, or `[::1]`; unknown/non-local hosts are blocked. |
| Should global `super_admin` manage users across all organizations or only same-org in current MVP? | Owner / Builder | Sprint 053-A implementation | Answered | Current MVP admin user routes enforce same-organization create/update behavior; broader global cross-org administration needs a future explicit policy. |
| How should the app define final Super Admin count? | Builder | Sprint 053-A implementation | Answered | The guard counts active users with active memberships normalized to `super_admin`; archived/suspended users and inactive memberships are excluded. |
| Is EmailORC intended for internal AI Hub use only, client demos, or paid client production use? | Owner | Production readiness sprint | Open | Affects auth, deployment, compliance, data retention, and sending rules. |
| What is the correct production target: Cloudflare only, local/server deploy, or another host? | Owner | Production readiness sprint | Open | Current audit history found Cloudflare D1 and OpenNext Cloudflare path. |
| Should direct email sending ever be enabled, or should the product remain review/export only? | Owner | Integration roadmap | Open | Current decision: no auto-send unless future sprint approves it. |
| Which exact Brain/provider routes are present in the repo? | Builder | Sprint 015 | Answered | `app/api/brain/api-key/route.ts`, `app/api/brain/embed/route.ts`, `app/api/brain/extract-knowledge/route.ts`, `app/api/brain/knowledge-search/route.ts`, `app/api/brain/learning-log/route.ts`, `app/api/brain/model-settings/route.ts`, `app/api/brain/models/route.ts`, `app/api/brain/regenerate-email/route.ts`, `app/api/brain/save-openrouter-key/route.ts`, `app/api/brain/test-chat/route.ts`, `app/api/brain/test-connection/route.ts`, and `app/api/brain/test-embedding/route.ts`. |
| Which Brain/provider routes expose provider keys, model settings, business knowledge, playbooks, learning logs, or prompt configuration? | Builder | Sprint 015 | Answered | All inspected `app/api/brain/*` routes except `extract-knowledge` were guarded because they read/write provider keys, model settings, embeddings/search, learning logs, usage/audit metadata, or live provider tests. |
| Do any Brain/provider-like APIs live outside obvious Brain/provider route folders? | Builder/Architect | Sprint 015 or future API inventory | Answered | Filename discovery found no `app/api/openrouter`, `app/api/provider`, `app/api/providers`, or top-level `app/api/model-settings` route files. Broader semantic API inventory remains future work. |
| Should billing/usage/account APIs be hardened in Sprint 016? | Architect/Owner | Sprint 016 planning | Open | Recommended next focused guard group after Brain/provider, unless page/middleware cleanup is prioritized. |
| When should middleware/page/localStorage cleanup happen? | Architect/Builder | Future page/session sprint | Open | Recommended after high-risk API route groups are guarded. |
| Why are medical/healthcare leads visible in the local app? | Builder | Sprint 072 | Answered | Repo evidence shows hardcoded demo records include `Rina Patel / BluePath Health` in `/mvp/records` and campaign-board seed UI data. Sprint 072 labels demo fallback records as `Demo fallback` and relies on app-visible import/source metadata to prove any persisted D1 origin. |
| Can a completed import be canceled safely in the current app? | Builder | Sprint 069 | Answered | No. Import persistence is immediate and `import_batches` has no cancellable status/lifecycle. Sprint 069 supports only canceling a staged upload before database save and draft generation. |
| Should archived leads be restorable or permanently hidden? | Owner / Architect | Sprint 072 | Answered | Sprint 072 safe defaults approve reversible lead archive with mandatory reason and audit. |
| Should completed imports support archive-only behavior, rollback behavior, both, or neither? | Owner / Architect | Sprint 072 | Answered | Sprint 072 safe defaults approve archive-only completed imports; rollback is deferred and destructive cleanup is prohibited. |
| Who may archive, restore, cancel, or roll back import batches? | Owner / Architect | Sprint 072 | Answered | Current uploader/import owner may cancel staged imports before persistence; admin/operator roles may archive and restore completed imports; rollback is not approved. |
| What reason categories are required for import archive, restore, cancel, and rollback? | Owner / Architect | Sprint 072 | Answered | Sprint 072 requires a fixed safe lifecycle reason for staged cancel, import archive/restore, and lead archive/restore. Rollback is not implemented. |
| Should import lifecycle events use the existing `audit_log` table or a dedicated structured lifecycle-events table? | Architect / Builder | Sprint 072 | Answered | Sprint 072 uses existing `audit_log`; schema inspection found it can carry actor, org, action, target, timestamp, and safe metadata. |
| Does the founder approve archive-only completed imports, rollback-only, both, or neither for Sprint 072? | Founder / Owner | Sprint 072 | Answered | Approved safe default is archive-only with rollback deferred. |
| Does the founder approve reversible lead archive, irreversible archive, or no lead archive feature? | Founder / Owner | Sprint 072 | Answered | Approved safe default is reversible lead archive. |
| What exact actor permissions are approved for staged cancel, import archive/restore, import rollback, and lead archive/restore? | Founder / Owner | Sprint 072 | Answered | Import owner/current uploader may cancel staged uploads; admin/operator roles may archive/restore completed imports and leads; rollback remains unapproved. |
| Which lifecycle actions require mandatory reason categories and optional safe notes? | Founder / Owner | Sprint 072 | Answered | Staged cancel, import archive/restore, and lead archive/restore require a reason. Optional note support exists server-side. |
| Should Sprint 072 use existing `audit_log`, a dedicated lifecycle-events table, or a hybrid approach? | Founder / Architect / Builder | Sprint 072 | Answered | Sprint 072 uses existing `audit_log` first. |
| Should lead archive use dedicated archive fields, continue `validation_status = ARCHIVED`, or use a separate archive table? | Founder / Architect / Builder | Sprint 072 | Answered | Sprint 072 adds migration text for dedicated lead archive fields and compatibility handling for older `validation_status = ARCHIVED` rows. |
