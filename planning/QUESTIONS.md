# Open Questions

| Question | Owner | Needed By | Status | Answer / Notes |
|---|---|---|---|---|
| Which Microsoft account will serve as the dedicated test mailbox? | Owner | Before OAuth testing | Open | Use one test mailbox only for Sprint 052. |
| Is the Entra app allowed to support personal Microsoft accounts, work/school accounts, or both? | Owner / Microsoft admin | Before implementation | Open | Default recommendation: support accounts in any organizational directory and personal Microsoft accounts, subject to the actual tenant/app-registration options. |
| Is user consent allowed for the chosen Microsoft tenant? | Owner / Microsoft admin | Before OAuth testing | Open | If tenant policy blocks user consent, an admin must grant consent for the delegated permission. |
| Where should encrypted connection material live in the deployed D1 design? | Architect / Builder | Before implementation | Open | Prefer a minimal D1-backed integration-connection table with encrypted payloads and rotation-ready key handling, if no existing secure store is already present. |
| Does the current runtime have a secure encryption-key binding pattern? | Builder | Preflight | Open | Inspect binding names/contracts only. Do not reveal secret values. |
| Is local validation using Next dev server, Wrangler local runtime, or a deployed test-live URL? | Owner / Builder | Before manual QA | Open | Choose one documented path that supports session, D1, OAuth callback, and safe test mailbox validation. |
| When should `0011_microsoft_outlook_drafts.sql` be applied to local/test D1? | Owner / Builder | Before manual OAuth QA | Open | Sprint 052 creates migration text only; application requires a separate approved D1 step. |
| Why did `/api/integrations/microsoft/connect` return `Authentication required.` under local Wrangler testing? | Builder | Sprint 053 implementation approval | Answered | Production-built preview set a Secure cookie on local HTTP, so the cookie was not sent back; local D1 also lacked the documented demo Super Admin row. |
| Is the local Super Admin user attached to the intended organization and active membership? | Builder | Sprint 053 implementation | Answered | Sprint 053 bootstraps the documented demo Super Admin into `org_demo` with `super_admin` only when missing in demo mode. |
| Why did Super Admin role updates fail to save locally? | Builder | Sprint 053 implementation | Answered | Without a recognized session, admin PATCH was not authorized. After session fix, same-org role update saved and persisted canonical `client_admin`. |
| Should local user creation require explicit role selection or keep a documented default role? | Owner / Builder | Sprint 053 implementation | Answered | Admin UI keeps explicit role selection with default `EDITOR`; no automatic `client_admin` assignment was added. |
| What exact local runtime signal should gate demo Super Admin bootstrap? | Builder | Sprint 053-A implementation | Answered | Bootstrap requires `APP_ENV=demo` and a local request host of `localhost`, `127.0.0.1`, or `[::1]`; unknown/non-local hosts are blocked. |
| Should global `super_admin` manage users across all organizations or only same-org in current MVP? | Owner / Builder | Sprint 053-A implementation | Answered | Current MVP admin user routes enforce same-organization create/update behavior; broader global cross-org administration needs a future explicit policy. |
| How should the app define final Super Admin count? | Builder | Sprint 053-A implementation | Answered | The guard counts active users with active memberships normalized to `super_admin`; archived/suspended users and inactive memberships are excluded. |
| Is EmailORC intended for internal AI Hub use only, client demos, or paid client production use? | Owner | Production readiness sprint | Open | Affects auth, deployment, compliance, data retention, and sending rules. |
| What is the correct production target: Cloudflare only, local/server deploy, or another host? | Owner | Production readiness sprint | Open | Current audit history found Cloudflare D1 and OpenNext Cloudflare path. |
| Should direct email sending ever be enabled, or should the product remain review/export only? | Owner | Integration roadmap | Open | Current decision: no auto-send unless future sprint approves it. |
| Which exact Brain/provider routes are present in the repo? | Builder | Sprint 015 | Answered | `app/api/brain/api-key/route.ts`, `app/api/brain/embed/route.ts`, `app/api/brain/extract-knowledge/route.ts`, `app/api/brain/knowledge-search/route.ts`, `app/api/brain/learning-log/route.ts`, `app/api/brain/model-settings/route.ts`, `app/api/brain/models/route.ts`, `app/api/brain/regenerate-email/route.ts`, `app/api/brain/save-openrouter-key/route.ts`, `app/api/brain/test-chat/route.ts`, `app/api/brain/test-connection/route.ts`, and `app/api/brain/test-embedding/route.ts`. |
| Which Brain/provider routes expose provider keys, model settings, business knowledge, playbooks, learning logs, or prompt configuration? | Builder | Sprint 015 | Answered | All inspected `app/api/brain/*` routes except `extract-knowledge` were guarded because they read/write provider keys, model settings, embeddings/search, learning logs, usage/audit metadata, or live provider tests. |
| Do any Brain/provider-like APIs live outside obvious Brain/provider route folders? | Builder/Architect | Sprint 015 or future API inventory | Answered | Filename discovery found no `app/api/openrouter`, `app/api/provider`, `app/api/providers`, or top-level `app/api/model-settings` route files. Broader semantic API inventory remains future work. |
| Should billing/usage/account APIs be hardened in Sprint 016? | Architect/Owner | Sprint 016 planning | Open | Recommended next focused guard group after Brain/provider, unless page/middleware cleanup is prioritized. |
| When should middleware/page/localStorage cleanup happen? | Architect/Builder | Future page/session sprint | Open | Recommended after high-risk API route groups are guarded. |
